%ifndef MAC_BLIT_MAC
%define MAC_BLIT_MAC

;=============================================================
; Macro that finishes an ongoing blit if registers are changed
;=============================================================


%macro BLITTEROPERATIONLOG_CWRAP 0
		CCALLCONV_IN_NONE
		call	blitterOperationLog
		CCALLCONV_OUT_NONE	0
%endmacro



%endif

